import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.vercel.aconcaguacup2022',
  appName: 'AconcaguaCup2022',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
